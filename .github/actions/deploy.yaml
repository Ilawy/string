jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install -g surge
      - run: npm install
      - run: npm run build # Or your build command
      - run: npm run deploy
      - run: surge ./dist string.osad.net --token ${{ secrets.SURGE_TOKEN }}
